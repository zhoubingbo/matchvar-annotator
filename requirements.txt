# MATCHVAR Annotator 1.2.0 — consumed by setup.py install_requires
# Python >= 3.8

# Core
pandas>=1.3.0
numpy>=1.21.0
psutil>=5.8.0

# Annotation I/O: VCF/tabix, genome FASTA, GTF
pysam>=0.19.0
pyfaidx>=0.5.8
biopython>=1.79

# Metrics and figures (AUROC, ClinVar ROC, pipeline plots)
matplotlib>=3.5.0
seaborn>=0.11.0
scikit-learn>=1.0.0
scipy>=1.7.0

# Optional: UCSC bigBed / MANE .bb  (pip install 'matchvar-annotator[bb]')
# pyBigWig>=0.3.18

# Dev / tests  (pip install -e '.[dev]')
# pytest>=6.0
# pytest-cov>=2.0
